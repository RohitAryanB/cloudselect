# Copy this file, fill in real values, and apply with kubectl.
# Do NOT commit the filled-in copy to git.
apiVersion: v1
kind: Secret
metadata:
  name: cloudselect-aws-credentials
type: Opaque
stringData:
  accessKeyId: "PASTE_AWS_ACCESS_KEY_ID_HERE"
  secretAccessKey: "PASTE_AWS_SECRET_ACCESS_KEY_HERE"
